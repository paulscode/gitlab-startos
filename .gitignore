builds/
*.s9pk
startos/*.js
node_modules/
.DS_Store
.vscode/
docker-images
javascript
ncc-cache

# Release signing: SHA256SUMS is generated into builds/ (already ignored) and
# signed off-box. Detached signatures never belong in the repo either.
*.asc
*.sig
*.gpg
